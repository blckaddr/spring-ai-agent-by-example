# Architecture

Living description of how the system is shaped. Updated as the build grows phase by phase
(see [`../../plans/`](../../plans/)). For *why* decisions were made, see [`../adr/`](../adr/).

## Documents

- [`overview.md`](overview.md) — components, boundaries, the agent loop, and how the picture
  grows across phases.

## Status

Pre-build. Nothing implemented yet. The diagrams below describe the **target**, reached
gradually — the codebase will lag this until each phase lands.
